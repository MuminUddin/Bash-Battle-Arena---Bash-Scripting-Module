# Bash Battle Arena Level 7  

**Mission**: Write a script that sorts all .txt files in a directory by their size, afrom smallest to largest, and displays the sorted list.  

**Solution**:  

``` bash
#!/bin/bash
